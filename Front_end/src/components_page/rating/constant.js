import classNames from 'classnames/bind';
import styles from './Rating.module.scss';

const cx = classNames.bind(styles);
const context = {
    ratingText: 'Đánh giá sản phẩm',
};

export { cx, context };
