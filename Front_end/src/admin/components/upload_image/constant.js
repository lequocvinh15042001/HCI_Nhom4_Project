import classNames from 'classnames/bind';
import styles from './UploadImage.module.scss';

export const cx = classNames.bind(styles);

export const context = {
    dragNDrop: 'Drap and drop your files or click',
};
